websocket: {
  # Specify the port for WebSocket connections
  port: 9222

  # Disable TLS
  no_tls: true

  # Optionally, enable compression
  # compression: true

  # Same origin policy
  # same_origin: true
}
